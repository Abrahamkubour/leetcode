class Solution:
    def findClosest(self, x: int, y: int, z: int) -> int:
         position_x = abs(x - z)
         position_y = abs(y - z)
         if position_x < position_y:
            return 1
         elif position_x > position_y:
            return 2
         else:
            return 0
